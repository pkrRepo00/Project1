<Window x:Class="FileConverterSuite.Presentation.Wpf.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="File Converter Suite" Height="720" Width="1100" AllowDrop="True">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="All-In-One File Conversion Suite" FontWeight="Bold" FontSize="26" />

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,16,0,16">
            <Button Content="Add Files" Width="110" Margin="0,0,8,0" Command="{Binding AddFilesCommand}"/>
            <Button Content="Convert All" Width="110" Margin="0,0,8,0" Command="{Binding ConvertAllCommand}"/>
            <Button Content="Toggle Theme" Width="120" Command="{Binding ToggleThemeCommand}"/>
        </StackPanel>

        <DataGrid Grid.Row="2" ItemsSource="{Binding Jobs}" AutoGenerateColumns="False" IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Input" Binding="{Binding InputPath}" Width="*"/>
                <DataGridTextColumn Header="Output" Binding="{Binding OutputPath}" Width="*"/>
                <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="140"/>
            </DataGrid.Columns>
        </DataGrid>

        <ProgressBar Grid.Row="3" Height="18" Margin="0,16,0,0" Value="{Binding Progress}" Maximum="100"/>
    </Grid>
</Window>
